"""Built-in openscout sport plugins (basketball, soccer).

Each submodule here registers itself through the same ``openscout.sports``
entry-point mechanism a third-party plugin would use -- see
``pyproject.toml`` and :mod:`openscout.plugins`.
"""
